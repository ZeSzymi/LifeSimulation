import { Defaults } from "../consts/environment";

export class Config {
    public bacterieAmount = Defaults.BACTERIE_AMOUNT;
    public foodAmount = Defaults.FOOD_AMOUNT;
    public foodOnRun = Defaults.FOOD_ON_RUN;
    public energy = Defaults.ENERGY;
    public light = Defaults.LIGHT;
}